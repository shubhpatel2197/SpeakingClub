{
  "name": "project-1",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "shared",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter @app/backend run dev\" \"pnpm --filter @app/frontend run dev\"",
    "build": "pnpm -r -w build",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "concurrently": "^8.2.0",
    "eslint": "^9.0.0",
    "prettier": "^3.2.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@prisma/client": "^6.17.0",
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.17.0"
  }
}
